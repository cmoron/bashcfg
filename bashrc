# ~/.bashrc: executed by bash(1) for non-login shells.

# === cmoron specific =============================================================================

# == Some aliases
alias ll='ls -l'
alias lla='ls -la'
alias l='ls -CF'
